# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

 use_frameworks!

target 'Recex' do
  pod "PromiseKit", "~> 6.8"
  pod 'lottie-ios'

  # Pods for Recex

  target 'RecexTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RecexUITests' do
    # Pods for testing
  end

end
